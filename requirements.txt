# Web framework
Flask==3.1.2
gunicorn==23.0.0

# NLP & ML
transformers==4.55.3
torch==2.8.0
scikit-learn==1.5.2
nltk==3.9.1

# Data handling
numpy==2.3.2
pandas==2.3.2

# File parsing
PyMuPDF==1.24.10      # provides `fitz` for PDFs
python-docx==1.1.2    # provides `docx` for Word files

# Utilities
tqdm==4.67.1          # used internally by transformers
requests==2.32.3      # safe version for HF model downloads
# Web app
Flask>=3.0.0,<4.0
gunicorn>=21.2,<22          # needed for Render; harmless on PythonAnywhere

# Resume parsing
PyMuPDF>=1.23,<2.0          # import name: fitz
python-docx>=1.0.0,<2.0

# NLP + ML stack
nltk>=3.8,<4.0
transformers>=4.38,<5.0
torch>=2.2,<3.0             # CPU wheel; no CUDA needed
scikit-learn>=1.3,<2.0
numpy>=1.24,<3.0
safetensors>=0.4,<1.0       # speeds up model loads
huggingface-hub>=0.22,<1.0  # used by transformers
tokenizers>=0.15,<0.20      # used by transformers
gunicorn==23.0.0
